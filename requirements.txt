# سيتم إضافة المكتبات هنا تدريجيًا مع كل مرحلة
# (مثال: requests, beautifulsoup4, sqlalchemy... إلخ)

# هذا الملف متروك للتوافق مع أدوات قديمة قد تتوقعه.
# المصدر الرسمي للـ dependencies هو pyproject.toml (استخدم: pip install -e ".[dev]")

pytest>=8.0.0
feedparser>=6.0.0
python-dotenv>=1.0.0
anthropic>=0.40.0
requests>=2.31.0
beautifulsoup4>=4.12.0
